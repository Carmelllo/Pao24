#include "mediavisitor.h"

MediaVisitor::MediaVisitor() {
    // Constructor definition (can be empty)
}

MediaVisitor::~MediaVisitor() {
    // Destructor definition (can be empty or with cleanup logic)
}
